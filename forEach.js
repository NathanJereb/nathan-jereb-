let colors = ["red", "blue", "yellow", "black", "green", "purple", "pink"]

function showColors(color) {
    console.log(color);
}

const powerrangers = colors.forEach( color => showColors(color))